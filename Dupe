local SG = Instance.new("ScreenGui", game.CoreGui) SG.Name = "FastResetGui"
local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()
local UIS = game:GetService("UserInputService")
local Players = game:GetService("Players")

-- CẤU HÌNH BIẾN TOÀN CỤC
_G.RespawnAtLocation = false
_G.SavedCFrame = nil

-- TÍNH NĂNG INSTANT E (CHẠY NGẦM, KHÔNG CẦN NÚT BẬT TẮT)
local function setInstantPrompt(obj)
    if obj:IsA("ProximityPrompt") then
        obj.HoldDuration = 0
    end
end
-- Chạy cho các vật phẩm đã có sẵn
for _, v in pairs(workspace:GetDescendants()) do
    setInstantPrompt(v)
end
-- Chạy cho các vật phẩm mới xuất hiện
game.DescendantAdded:Connect(setInstantPrompt)

local Window = Rayfield:CreateWindow({
    Name = "Script Hub Của Bạn", LoadingTitle = "loading", LoadingSubtitle = "❤️‍🔥MIKEY❤️‍🔥",
    ConfigurationSaving = { Enabled = true, FolderName = "MIKEYDUPE", FileName = "Config" },
    Discord = { Enabled = false, Invite = "noinvitelink", RememberJoins = true }, KeySystem = false, 
})

-- TAB 1: MAIN
local MainTab = Window:CreateTab("Main", 4483362458) 

MainTab:CreateToggle({
    Name = "Auto Cash", CurrentValue = false,
    Callback = function(Value)
        _G.AutoCash = Value
        while _G.AutoCash do
            task.wait(0.1) 
            pcall(function()
                local myPlot = workspace:FindFirstChild("Plots") and workspace.Plots:FindFirstChild(Players.LocalPlayer.Name)
                local stands = myPlot and myPlot:FindFirstChild("AnimalStands")
                if stands then
                    for i = 1, 40 do
                        if not _G.AutoCash then break end
                        local s = stands:FindFirstChild(tostring(i))
                        if s then game:GetService("ReplicatedStorage"):WaitForChild("Events"):WaitForChild("CollectCash"):FireServer(s) end
                    end
                end
            end)
        end
    end,
})

MainTab:CreateToggle({
    Name = "Auto Drop", CurrentValue = false,
    Callback = function(Value)
        _G.AutoDrop = Value
        while _G.AutoDrop do
            task.wait(0.1)
            pcall(function()
                local myPlot = workspace:FindFirstChild("Plots") and workspace.Plots:FindFirstChild(Players.LocalPlayer.Name)
                local stands = myPlot and myPlot:FindFirstChild("AnimalStands")
                if stands then
                    for i = 1, 40 do
                        if not _G.AutoDrop then break end
                        local s = stands:FindFirstChild(tostring(i))
                        if s then game:GetService("ReplicatedStorage"):WaitForChild("Events"):WaitForChild("InteractStand"):FireServer(s) end
                    end
                end
            end)
        end
    end,
})

MainTab:CreateToggle({
    Name = "Auto Rebirth", CurrentValue = false,
    Callback = function(Value)
        _G.AutoRebirth = Value
        while _G.AutoRebirth do
            task.wait(1) 
            pcall(function() game:GetService("ReplicatedStorage"):WaitForChild("Events"):WaitForChild("Rebirth"):FireServer() end)
        end
    end
})

MainTab:CreateToggle({
    Name = "Auto Buy Speed", CurrentValue = false,
    Callback = function(Value)
        _G.AutoBuySpeed = Value
        while _G.AutoBuySpeed do
            task.wait(0.01) 
            pcall(function() game:GetService("ReplicatedStorage"):WaitForChild("Events"):WaitForChild("BuySpeed"):FireServer(5) end)
        end
    end
})

-- TAB 2: UTILITY
local UtilityTab = Window:CreateTab("Utility", 4483362458)
UtilityTab:CreateToggle({
    Name = "Hồi sinh tại chỗ cũ", CurrentValue = false,
    Callback = function(Value) _G.RespawnAtLocation = Value end,
})

-- HÀM RESET DÙNG CHUNG
local function performReset()
    local p = Players.LocalPlayer
    if p.Character and p.Character:FindFirstChild("HumanoidRootPart") and p.Character:FindFirstChild("Humanoid") then
        if _G.RespawnAtLocation then _G.SavedCFrame = p.Character.HumanoidRootPart.CFrame end
        p.Character.Humanoid.Health = 0
    end
end

-- LOGIC HỒI SINH
Players.LocalPlayer.CharacterAdded:Connect(function(newChar)
    if _G.RespawnAtLocation and _G.SavedCFrame then
        task.wait(0.5)
        local hrp = newChar:WaitForChild("HumanoidRootPart", 5)
        if hrp then hrp.CFrame = _G.SavedCFrame end
    end
end)

-- NÚT RESET TRÊN MÀN HÌNH
local RB = Instance.new("TextButton", SG) local UC = Instance.new("UICorner", RB)
RB.Name = "ResetButton" RB.BackgroundColor3 = Color3.fromRGB(255, 50, 50)
RB.Position = UDim2.new(0.05, 0, 0.4, 0) RB.Size = UDim2.new(0, 50, 0, 50) 
RB.Font = Enum.Font.GothamBold RB.Text = "R" RB.TextColor3 = Color3.fromRGB(255, 255, 255)
RB.TextSize = 24 RB.ZIndex = 999 UC.CornerRadius = UDim.new(1, 0)

-- Di chuyển nút
local dragging, dragStart, startPos
RB.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragging, dragStart, startPos = true, input.Position, RB.Position
    end
end)
UIS.InputChanged:Connect(function(input)
    if dragging then
        local delta = input.Position - dragStart
        RB.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
    end
end)
UIS.InputEnded:Connect(function(input) dragging = false end)

-- Sự kiện nhấn
RB.MouseButton1Click:Connect(performReset)
UIS.InputBegan:Connect(function(input, gameProcessed)
    if not gameProcessed and input.KeyCode == Enum.KeyCode.R then
        performReset()
    end
end)
